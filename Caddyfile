{
    debug
    	log default {
		output stdout
		format json
        }
}

:80 {
    root * /var/www/html
    file_server

    handle_path /openbis/* {
        rewrite * /openbis{path}
        reverse_proxy {$OPENBIS_URL}
    }
}