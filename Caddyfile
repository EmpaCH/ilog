{
    debug
    	log default {
		output stdout
		format json
        }
}

:80 {
    root * /var/www/html
    file_server

    handle_path /openbis/* {
        reverse_proxy {$OPENBIS_URL}/openbis/
    }
}